#include<stdio.h>
int main(){
	// khai bao bien
	int number;
	// nhap du lieu
	printf(" moi nhap mot so nguyen ");
	scanf("%d", &number);
	// dieu kien
	if (number > 0){
		// in ra man hinh
		printf(" la so duong: %d", number); 
		
	}
	else{
		printf(" la so am: %d", number);
	}
	// ket thuc
	
	
	
	
	return 0;
}
